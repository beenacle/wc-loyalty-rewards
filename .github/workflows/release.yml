name: Build and Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Build plugin zip
        run: |
          mkdir -p build/wc-loyalty-rewards
          rsync -a --exclude='.git' --exclude='.github' --exclude='composer.phar' --exclude='*.log' ./ build/wc-loyalty-rewards/
          cd build && zip -rq wc-loyalty-rewards.zip wc-loyalty-rewards/

      - name: Create release
        uses: softprops/action-gh-release@v2
        with:
          files: build/wc-loyalty-rewards.zip
          generate_release_notes: true
